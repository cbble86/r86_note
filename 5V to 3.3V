Altera PLD can be set working at 3.3V PCI， io pins include a clamping diode， use a resistor between fpga and 5V IC.
For example, the original 80386 have 5v ttl pins， set fpga io pins as PCI_PCI-X 3.3v， and use a hundred Ohm res，
the signal volt is suitable.
